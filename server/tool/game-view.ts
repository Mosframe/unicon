// -----------------------------------------------------------------------------
// scene-view.ts
// -----------------------------------------------------------------------------
import deprecated     from 'deprecated-decorator';
import * as GL        from '../engine/graphic';
import {EditorWindow} from '../editor/editor-window';

/**
 * The Game View is rendered from the Camera(s) in your game.
 * It is representative of your final, published game.
 * You will need to use one or more Cameras to control what the player actually sees when they are playing your game.
 * For more information about Cameras, please view the Camera Component page.
 *
 * @author mosframe / https://github.com/mosframe
 * @export
 * @class GameView
 * @extends {EditorWindow}
 */
export class GameView extends EditorWindow {

    // [ Public Variables ]

    // [ Constructors ]

    // [ Public Functions ]

    // [ Public Static Variables ]

    // [ Public Static Functions ]

    // [ Protected Variables ]

    // [ Protected Functions ]

    // [ Protected Static Variables ]

    // [ Protected Static Functions ]
}
